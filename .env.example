# ==============================================================================
# VLAN Manager - Environment Configuration (NetBox Backend)
# ==============================================================================

# ------------------------------------------------------------------------------
# NetBox Connection (Required)
# ------------------------------------------------------------------------------
# Your NetBox instance URL
NETBOX_URL=https://your-netbox-instance.com

# NetBox API authentication token
# Generate this in NetBox: User Menu → API Tokens → Add Token
NETBOX_TOKEN=your-api-token-here

# SSL Certificate Verification (Optional)
# Set to "false" if using self-signed certificates
NETBOX_SSL_VERIFY=true

# ------------------------------------------------------------------------------
# Site Configuration (Required)
# ------------------------------------------------------------------------------
# Comma-separated list of site names
# These must match sites configured in NetBox
SITES=site1,site2,site3

# Site IP Prefix Mapping (Required)
# Format: site1:prefix1,site2:prefix2
# Example: site1:192 means site1 uses 192.x.x.x addresses
SITE_PREFIXES=site1:192,site2:193,site3:194

# ------------------------------------------------------------------------------
# Server Configuration (Optional)
# ------------------------------------------------------------------------------
# Server bind address (default: 0.0.0.0)
SERVER_HOST=0.0.0.0

# Server port (default: 8000)
SERVER_PORT=8000

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ------------------------------------------------------------------------------
# Notes
# ------------------------------------------------------------------------------
# 1. All sites in SITES must have corresponding entries in SITE_PREFIXES
# 2. Application will fail at startup if configuration is incomplete
# 3. NetBox connection is verified during startup
# 4. Data is stored in NetBox (no local file storage needed)
# 5. For production: Use environment-specific values, not this template
