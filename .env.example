# ==============================================================================
# VLAN Manager - Environment Configuration (MySQL Backend)
# ==============================================================================

# ------------------------------------------------------------------------------
# Storage Backend Selection (Required)
# ------------------------------------------------------------------------------
# Choose storage backend: "mysql" (recommended) or "netbox" (legacy)
STORAGE_BACKEND=mysql

# ------------------------------------------------------------------------------
# MySQL Configuration (Required for MySQL backend)
# ------------------------------------------------------------------------------
# MySQL database server hostname
MYSQL_HOST=localhost

# MySQL database server port
MYSQL_PORT=3306

# MySQL database name
MYSQL_DATABASE=vlan_manager

# MySQL database user
MYSQL_USER=vlan_manager

# MySQL database password
MYSQL_PASSWORD=vlan_manager_password

# MySQL connection pool size (default: 20)
MYSQL_POOL_SIZE=20

# ------------------------------------------------------------------------------
# NetBox Configuration (Legacy - Optional)
# ------------------------------------------------------------------------------
# Keep for reference if switching back to NetBox backend
NETBOX_URL=https://your-netbox-instance.com
NETBOX_TOKEN=your-api-token-here
NETBOX_SSL_VERIFY=true

# ------------------------------------------------------------------------------
# Site Configuration (Required)
# ------------------------------------------------------------------------------
# Comma-separated list of site names
SITES=Site1,Site2,Site3

# Site IP Prefix Mapping (Required)
# Format: site1:prefix1,site2:prefix2
# Example: Site1:192 means Site1 uses 10.192.x.x addresses
SITE_PREFIXES=Site1:192,Site2:193,Site3:194

# ------------------------------------------------------------------------------
# Server Configuration (Optional)
# ------------------------------------------------------------------------------
# Server bind address (default: 0.0.0.0)
HOST=0.0.0.0

# Server port (default: 9000)
PORT=9000

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Environment: development or production
ENVIRONMENT=development

# CORS origins (comma-separated, for frontend integration)
CORS_ORIGINS=http://localhost:3000,http://localhost:9000

# ------------------------------------------------------------------------------
# Notes
# ------------------------------------------------------------------------------
# 1. MySQL backend is the default and recommended storage
# 2. All sites in SITES must have corresponding entries in SITE_PREFIXES
# 3. Application will fail at startup if configuration is incomplete
# 4. MySQL database and tables will be created automatically on first run
# 5. For production: Use strong passwords and secure database access
# 6. For Docker: Set MYSQL_HOST to service name (e.g., "mysql")
