---
phase: 02-validation-rationalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/validators/security_validators.py
  - src/utils/validators/organization_validators.py
  - src/utils/validators/data_validators.py
  - src/utils/validators/__init__.py
  - src/services/segment_service.py
autonomous: true
requirements:
  - VAL-01
  - VAL-02
  - VAL-03
  - VAL-04

must_haves:
  truths:
    - "No file named security_validators.py exists anywhere under src/"
    - "No import of SecurityValidators exists in any file under src/"
    - "No call to Validators.validate_no_script_injection exists in any file under src/"
    - "No NoSQL injection checks ($, __proto__, constructor) exist in data_validators.py"
    - "No dead validator methods (sanitize_input, validate_no_path_traversal, validate_rate_limit_data, validate_concurrent_modification, validate_json_serializable, validate_timezone_aware_datetime, validate_update_data) exist in the validators package"
    - "The application starts without ImportError or AttributeError"
  artifacts:
    - path: "src/utils/validators/__init__.py"
      provides: "Validators facade class — SecurityValidators import and all dead staticmethod assignments removed"
    - path: "src/utils/validators/organization_validators.py"
      provides: "validate_concurrent_modification method removed"
    - path: "src/utils/validators/data_validators.py"
      provides: "validate_timezone_aware_datetime, validate_json_serializable, validate_update_data methods removed"
    - path: "src/services/segment_service.py"
      provides: "Both validate_no_script_injection call sites removed"
  key_links:
    - from: "src/utils/validators/__init__.py"
      to: "src/utils/validators/security_validators.py"
      via: "import statement"
      pattern: "from .security_validators import SecurityValidators"
    - from: "src/services/segment_service.py"
      to: "src/utils/validators/__init__.py"
      via: "Validators.validate_no_script_injection calls"
      pattern: "validate_no_script_injection"
---

<objective>
Remove the wrong-threat-model security validators and all dead code validators from the validation layer.

Purpose: The security validators (XSS, path traversal, rate limiting) are theater for an internal REST API tool; the NoSQL injection checks are MongoDB-specific patterns meaningless against pynetbox. Removing them reduces ~200 lines of maintenance burden and eliminates the only two validator call sites in segment_service.py that actively block legitimate operator input.

Output: security_validators.py deleted; dead methods removed from organization_validators.py and data_validators.py; __init__.py cleaned of corresponding imports and staticmethod assignments; segment_service.py cleaned of the two live call sites.
</objective>

<execution_context>
@/Users/roiblum/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roiblum/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-validation-rationalization/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead validators from organization_validators.py, data_validators.py, and __init__.py</name>
  <files>
    src/utils/validators/organization_validators.py
    src/utils/validators/data_validators.py
    src/utils/validators/__init__.py
  </files>
  <action>
    In organization_validators.py:
    - Remove the entire `validate_concurrent_modification` staticmethod from the OrganizationValidators class body. It has zero external call sites.

    In data_validators.py:
    - Remove the entire `validate_timezone_aware_datetime` staticmethod.
    - Remove the entire `validate_json_serializable` staticmethod.
    - Remove the entire `validate_update_data` staticmethod. It has zero external call sites (confirmed: no route or service calls it; it only existed as a facade assignment in __init__.py).
    - After removing those three methods, remove any imports that were only used by them (e.g., `timezone`, `json` if no other method uses them — verify before removing).

    In __init__.py (Validators class body), remove these 7 staticmethod assignments:
    ```python
    validate_concurrent_modification = staticmethod(OrganizationValidators.validate_concurrent_modification)
    validate_timezone_aware_datetime = staticmethod(DataValidators.validate_timezone_aware_datetime)
    validate_json_serializable = staticmethod(DataValidators.validate_json_serializable)
    validate_update_data = staticmethod(DataValidators.validate_update_data)
    ```
    (The security-related staticmethod assignments will be removed in Task 2 along with the file deletion.)

    Do NOT remove any other methods — only the confirmed-dead ones listed above.
    Do NOT remove DataValidators or OrganizationValidators from __init__.py imports if they still have surviving methods.
  </action>
  <verify>
    python -c "from src.utils.validators import Validators; print('import OK')"
    grep -r "validate_concurrent_modification\|validate_timezone_aware_datetime\|validate_json_serializable\|validate_update_data" src/ --include="*.py"
  </verify>
  <done>
    The four dead methods are absent from all files under src/. The import statement above succeeds with no AttributeError. grep returns no matches for any of the four function names.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete security_validators.py and clean all references from __init__.py and segment_service.py</name>
  <files>
    src/utils/validators/security_validators.py
    src/utils/validators/__init__.py
    src/services/segment_service.py
  </files>
  <action>
    In segment_service.py, remove the two live call sites BEFORE deleting the module (prevents runtime breakage):
    - Remove line ~34: `Validators.validate_no_script_injection(segment.description, "description")`
    - Remove line ~37: `Validators.validate_no_script_injection(segment.epg_name, "epg_name")`
    - Remove the comment line preceding each call (# Description validation (XSS protection), # EPG name XSS protection). Do not remove other validation calls in _validate_segment_data.

    In __init__.py:
    - Remove the import at the top of the file: `from .security_validators import SecurityValidators`
    - Remove these 4 staticmethod assignments from the Validators class body:
      ```python
      sanitize_input = staticmethod(SecurityValidators.sanitize_input)
      validate_no_script_injection = staticmethod(SecurityValidators.validate_no_script_injection)
      validate_no_path_traversal = staticmethod(SecurityValidators.validate_no_path_traversal)
      validate_rate_limit_data = staticmethod(SecurityValidators.validate_rate_limit_data)
      ```
    - Remove `"SecurityValidators"` from the `__all__` list.

    Delete the file:
    - Delete src/utils/validators/security_validators.py entirely (use Bash rm or Write with empty content is not appropriate — use Bash to delete).

    Verify no other file under src/ imports from security_validators before deleting.
  </action>
  <verify>
    python -c "from src.utils.validators import Validators; print('import OK')"
    python -c "from src.services.segment_service import SegmentService; print('service import OK')"
    ls src/utils/validators/security_validators.py 2>/dev/null && echo "FILE STILL EXISTS - FAIL" || echo "File deleted OK"
    grep -r "security_validators\|SecurityValidators\|validate_no_script_injection\|sanitize_input\|validate_no_path_traversal\|validate_rate_limit_data" src/ --include="*.py"
  </verify>
  <done>
    security_validators.py does not exist. All four grep patterns return zero matches across src/. Both import statements succeed without error.
  </done>
</task>

</tasks>

<verification>
From the project root with the virtual environment activated:

1. Confirm file is gone: `ls src/utils/validators/security_validators.py` → "No such file or directory"
2. Confirm no references remain: `grep -r "security_validators\|SecurityValidators\|validate_no_script_injection\|validate_concurrent_modification\|validate_update_data\|validate_json_serializable\|validate_timezone_aware_datetime" src/ --include="*.py"` → zero matches
3. Confirm validator package imports cleanly: `python -c "from src.utils.validators import Validators; print(dir(Validators))"`
4. Confirm segment service imports cleanly: `python -c "from src.services.segment_service import SegmentService; print('OK')"`
5. Confirm application starts: `python -c "import src.app; print('app import OK')"`
</verification>

<success_criteria>
- security_validators.py is deleted and absent from the repository
- grep finds zero references to SecurityValidators, validate_no_script_injection, sanitize_input, validate_no_path_traversal, validate_rate_limit_data across all .py files
- grep finds zero references to validate_concurrent_modification, validate_update_data, validate_json_serializable, validate_timezone_aware_datetime across all .py files
- The validators package imports without error
- The segment service imports without error
- The application module imports without error
</success_criteria>

<output>
After completion, create `.planning/phases/02-validation-rationalization/02-01-SUMMARY.md`
</output>
