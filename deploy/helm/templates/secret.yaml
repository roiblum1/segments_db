{{- if .Values.mongodb.secret.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.mongodb.secret.name | default (printf "%s-mongodb" (include "vlan-manager.fullname" .)) }}
  labels:
    {{- include "vlan-manager.labels" . | nindent 4 }}
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
type: Opaque
data:
  # MongoDB connection string (base64 encoded)
  MONGODB_URL: {{ .Values.mongodb.secret.url | b64enc | quote }}
{{- end }}