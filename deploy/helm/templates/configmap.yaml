apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "vlan-manager.fullname" . }}-config
  labels:
    {{- include "vlan-manager.labels" . | nindent 4 }}
data:
  sites.conf: |
    {{- .Values.env.SITES | nindent 4 }}